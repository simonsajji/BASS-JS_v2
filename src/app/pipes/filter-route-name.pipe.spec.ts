import { FilterRouteNamePipe } from './filter-route-name.pipe';

describe('FilterRouteNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRouteNamePipe();
    expect(pipe).toBeTruthy();
  });
});
